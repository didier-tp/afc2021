logiciels à installer (sur le pc) d'ici jeudi/vendredi:
==================================
keycloak (serveur oauth2/oidc)
nodejs en version récente LTS et sa sous partie npm (pour lancer application angular)
==========================

1. virtualbox (pour faire fonctionner linux sous windows)
2. vagrant (pour configurer et démmarrer facilement virtualbox et linux en mode texte)
3. https://github.com/didier-mycontrib/msa-vagrant (à récupérer via git clone)
   et projet va-sta-msa-kong et lancer script vagant-up-cmd.bat


Puis via vagrant/linux,
on pourra installer/configurer:
  - docker et docker-compose
  - kong (api-gateway)
  - ...
